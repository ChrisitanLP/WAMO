<odoo>
    <data>
        <!-- Acción que redirige a la ruta -->
        <record id="action_message_app" model="ir.actions.act_url">
            <field name="name">App WhatsApp</field>
            <field name="url">/whatsapp/menu_opciones</field>
            <field name="target">self</field>
        </record>

        <!-- Menú principal -->
        <menuitem id="menu_whatsapp_api" name="App WhatsApp" sequence="10" action="action_message_app"/>
    </data>
</odoo>